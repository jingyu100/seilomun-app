import React, { useRef, useEffect, useState } from 'react';
import { useRoute } from "@react-navigation/native";
import {
    StyleSheet,
    View,
    Text,
    Animated,
    Image,
    ScrollView,
    SafeAreaView,
    TouchableOpacity,
    Dimensions,
    Alert,
} from 'react-native';
import api, { S3_BASE_URL } from '../../../../api/api.js';

export default function PayResultModal() {

    

    return(
        <SafeAreaView>

        </SafeAreaView>
    )
}